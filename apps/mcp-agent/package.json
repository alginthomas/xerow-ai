{
  "name": "xerow-mcp-agent",
  "version": "1.0.0",
  "description": "MCP Commerce Agent - Python Implementation",
  "scripts": {
    "setup": "python3 -m venv env && source env/bin/activate && pip install -r requirements.txt",
    "dev": "bash run_agent.sh"
  },
  "dependencies": {},
  "devDependencies": {}
}
